/*global define*/

define([
    'backbone',
    'backbone.marionette',
    'underscore',
    'jquery'

], function(Backbone, Marionette, _, $) {
    'use strict';
    var HeaderView = Marionette.ItemView.extend({

    });
});